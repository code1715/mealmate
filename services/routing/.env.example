# Neo4j connection
# In Docker Compose the hostname is "neo4j" (the Docker service name).
# For local development outside Docker, use "localhost".

NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=mealmate
